dist/
.build/
*.xcarchive
*.ipa
*.apk
*.wasm
.DS_Store
.infisical.json
# Default local output from scripts/provision-release-secrets.sh. It contains
# release signing and Android keystore credentials and must never be staged.
axiom-ui-host-release-secrets.env
